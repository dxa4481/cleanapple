# Linker configuration for Apple II Monitor ROM
# ROM occupies $F800-$FFFF (2KB)

MEMORY {
    ROM: start = $F800, size = $0800, file = %O, fill = yes, fillval = $FF;
}

SEGMENTS {
    MONITOR: load = ROM, type = ro;
}
